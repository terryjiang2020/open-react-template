-- Table: Users
[Table Purpose]
Stores user account and profile information.
[Primary Key]
id
[Foreign Keys]
None

id SERIAL PRIMARY KEY,
role_id INT, -- 1: Admin, 2: User
username VARCHAR(100),
email VARCHAR(200),
email_verified BOOLEAN DEFAULT FALSE,
validating_code VARCHAR(6),
phone_number VARCHAR(20),
password TEXT,
business_name VARCHAR(100),
gst_number VARCHAR(20),
nzbn VARCHAR(20),
company_address_line_1 VARCHAR(200),
company_address_line_2 VARCHAR(200),
company_city VARCHAR(100),
company_state VARCHAR(100),
company_zip_code VARCHAR(20),
company_country_id INT,
agent_number INT DEFAULT 1,
full_name VARCHAR(100),
photo_url VARCHAR(200),
country VARCHAR(100),
logged_in BOOLEAN DEFAULT FALSE,
last_active TIMESTAMP DEFAULT NOW(),
last_login_time TIMESTAMP DEFAULT NOW(),
active BOOLEAN DEFAULT TRUE,
deleted BOOLEAN DEFAULT FALSE,
created_at TIMESTAMP DEFAULT NOW(),
created_by INT,
updated_at TIMESTAMP DEFAULT NOW(),
updated_by INT

-- Table: BillingAddresses
[Table Purpose]
Stores billing address information for users.
[Primary Key]
id
[Foreign Keys]
user_id → Users.id

id SERIAL PRIMARY KEY,
user_id INT,
full_name VARCHAR(100),
phone_number VARCHAR(20),
address_line_1 VARCHAR(200),
address_line_2 VARCHAR(200),
city VARCHAR(100),
state VARCHAR(100),
zip_code VARCHAR(20),
country_id INT,
deleted BOOLEAN DEFAULT FALSE,
created_at TIMESTAMP DEFAULT NOW(),
created_by INT,
updated_at TIMESTAMP DEFAULT NOW(),
updated_by INT

-- Table: CreditCards
[Table Purpose]
Stores credit card information for users.
[Primary Key]
id
[Foreign Keys]
user_id → Users.id

id SERIAL PRIMARY KEY,
user_id INT,
last_4_digits VARCHAR(20),
stripe_customer_id VARCHAR(100),
stripe_card_id VARCHAR(100),
deleted BOOLEAN DEFAULT FALSE,
created_at TIMESTAMP DEFAULT NOW(),
created_by INT,
updated_at TIMESTAMP DEFAULT NOW(),
updated_by INT

-- Table: UserApiTokens
[Table Purpose]
Stores API tokens for users.
[Primary Key]
id
[Foreign Keys]
user_id → Users.id

id SERIAL PRIMARY KEY,
user_id INT,
token VARCHAR(200),
status INT DEFAULT 1, -- 1: Active, 2: Inactive
deleted BOOLEAN DEFAULT FALSE,
created_at TIMESTAMP DEFAULT NOW(),
created_by INT NOT NULL,
updated_at TIMESTAMP DEFAULT NOW(),
updated_by INT NOT NULL

-- Table: EmailVerifyLinks
[Table Purpose]
Stores email verification links for users.
[Primary Key]
id
[Foreign Keys]
user_id → Users.id

id SERIAL PRIMARY KEY NOT NULL,
email VARCHAR(200),
new_email VARCHAR(200),
url VARCHAR(200),
code VARCHAR(6),
user_id INT,
user_type INT DEFAULT 1,
used BOOLEAN DEFAULT FALSE,
deleted BOOLEAN DEFAULT FALSE,
valid_before TIMESTAMP NOT NULL,
created_at TIMESTAMP NOT NULL DEFAULT NOW(),
updated_at TIMESTAMP NOT NULL DEFAULT NOW()

-- Table: Roles
[Table Purpose]
Stores user roles.
[Primary Key]
id
[Foreign Keys]
None

id SERIAL PRIMARY KEY,
name TEXT,
deleted BOOLEAN DEFAULT FALSE,
created_at TIMESTAMP DEFAULT NOW(),
created_by INT DEFAULT 0,
updated_at TIMESTAMP DEFAULT NOW(),
updated_by INT DEFAULT 0

-- Table: ResetPasswordUrls
[Table Purpose]
Stores password reset URLs for users.
[Primary Key]
id
[Foreign Keys]
user_id → Users.id

id SERIAL PRIMARY KEY,
user_id INT NOT NULL,
alternative_id INT,
url VARCHAR(200) NOT NULL,
valid BOOLEAN DEFAULT TRUE,
created_at TIMESTAMP DEFAULT NOW(),
created_by INT,
updated_at TIMESTAMP DEFAULT NOW(),
updated_by INT

-- Table: UnsubscriptionUniqueUrls
[Table Purpose]
Stores unique unsubscription URLs for users.
[Primary Key]
id
[Foreign Keys]
user_id → Users.id

id SERIAL PRIMARY KEY,
email VARCHAR(200) NOT NULL,
url VARCHAR(200) NOT NULL,
user_id INT,
used BOOLEAN DEFAULT FALSE,
user_type INT DEFAULT 2, -- 2: User, 1: Undefined
disabled BOOLEAN DEFAULT FALSE,
reason_id INT,
content TEXT,
created_at TIMESTAMP DEFAULT NOW(),
created_by INT,
updated_at TIMESTAMP DEFAULT NOW(),
updated_by INT

-- Table: UserProfiles
[Table Purpose]
Stores user profile information.
[Primary Key]
id
[Foreign Keys]
user_id → Users.id

id SERIAL PRIMARY KEY,
user_id INT,
current_role_level_id INT,
current_job_title VARCHAR(100),
desired_annual_salary VARCHAR(50),
desired_country_id INT,
resume_url VARCHAR(200),
linkedin_url VARCHAR(200),
deleted BOOLEAN DEFAULT FALSE,
created_at TIMESTAMP DEFAULT NOW(),
created_by INT DEFAULT 0,
updated_at TIMESTAMP DEFAULT NOW(),
updated_by INT DEFAULT 0

-- Table: Notifications
[Table Purpose]
Stores notifications for users.
[Primary Key]
id
[Foreign Keys]
user_id → Users.id

id SERIAL PRIMARY KEY,
user_id INT,
type_id INT,
item_id INT,
title VARCHAR(100),
content TEXT,
read BOOLEAN DEFAULT FALSE,
deleted BOOLEAN DEFAULT FALSE,
created_at TIMESTAMP DEFAULT NOW(),
created_by INT DEFAULT 0,
updated_at TIMESTAMP DEFAULT NOW(),
updated_by INT DEFAULT 0

-- Table: Plans
[Table Purpose]
Stores subscription plans.
[Primary Key]
id
[Foreign Keys]
None

id SERIAL PRIMARY KEY,
name VARCHAR(100),
description TEXT,
price DECIMAL(10, 2),
currency VARCHAR(10),
period INT, -- 1: Monthly, 2: Quarterly, 3: Yearly
deleted BOOLEAN DEFAULT FALSE,
created_at TIMESTAMP DEFAULT NOW(),
created_by INT DEFAULT 0,
updated_at TIMESTAMP DEFAULT NOW(),
updated_by INT DEFAULT 0

-- Table: Subscriptions
[Table Purpose]
Stores user subscriptions to plans.
[Primary Key]
id
[Foreign Keys]
user_id → Users.id
plan_id → Plans.id

id SERIAL PRIMARY KEY,
user_id INT,
plan_id INT,
subscription_start_date DATE,
subscription_end_date DATE,
subscription_status INT, -- 1: Active, 2: Inactive
deleted BOOLEAN DEFAULT FALSE,
created_at TIMESTAMP DEFAULT NOW(),
created_by INT DEFAULT 0,
updated_at TIMESTAMP DEFAULT NOW(),
updated_by INT DEFAULT 0

-- Table: EmailAuthorizations
[Table Purpose]
Stores email authorization codes for users.
[Primary Key]
id
[Foreign Keys]
None

id SERIAL PRIMARY KEY,
email VARCHAR(200),
platform_id INT, -- 1: Gmail, 2: Outlook
code VARCHAR(6),
valid_before TIMESTAMP,
used BOOLEAN DEFAULT FALSE,
deleted BOOLEAN DEFAULT FALSE,
created_at TIMESTAMP DEFAULT NOW(),
created_by INT DEFAULT 0,
updated_at TIMESTAMP DEFAULT NOW(),
updated_by INT DEFAULT 0

-- Table: UserCreditBalances
[Table Purpose]
Stores user credit balances.
[Primary Key]
id
[Foreign Keys]
user_id → Users.id

id SERIAL PRIMARY KEY,
user_id INT REFERENCES Users(id),
amount INT DEFAULT 0, -- USD Cents
deleted BOOLEAN DEFAULT FALSE,
created_at TIMESTAMP DEFAULT NOW(),
created_by INT DEFAULT 0,
updated_at TIMESTAMP DEFAULT NOW(),
updated_by INT DEFAULT 0

-- Table: UserCreditBalanceHistories
[Table Purpose]
Stores user credit balance history.
[Primary Key]
id
[Foreign Keys]
user_id → Users.id

id SERIAL PRIMARY KEY,
user_id INT REFERENCES Users(id),
amount INT DEFAULT 0, -- USD Cents
message TEXT,
deleted BOOLEAN DEFAULT FALSE,
created_at TIMESTAMP DEFAULT NOW(),
created_by INT DEFAULT 0,
updated_at TIMESTAMP DEFAULT NOW(),
updated_by INT DEFAULT 0

-- Table: UserPlans
[Table Purpose]
Stores available user plans.
[Primary Key]
id
[Foreign Keys]
None

id SERIAL PRIMARY KEY,
title TEXT,
price INT DEFAULT 0,
amount INT DEFAULT 0,
purchase_link VARCHAR(200),
stripe_prod_id VARCHAR(100),
hidden BOOLEAN DEFAULT FALSE,
deleted BOOLEAN DEFAULT FALSE,
created_at TIMESTAMP DEFAULT NOW(),
created_by INT DEFAULT 0,
updated_at TIMESTAMP DEFAULT NOW(),
updated_by INT DEFAULT 0

-- Table: UserPlanSubscriptions
[Table Purpose]
Stores user subscriptions to plans.
[Primary Key]
id
[Foreign Keys]
user_id → Users.id
plan_id → UserPlans.id

id SERIAL PRIMARY KEY,
user_id INT REFERENCES Users(id),
plan_id INT REFERENCES UserPlans(id),
stripe_event_id VARCHAR(100),
stripe_subscription_id VARCHAR(100),
start_date DATE,
end_date DATE,
amount INT DEFAULT 0,
status INT DEFAULT 1, -- 0: Paused, 1: Active, 2: Cancelled
deleted BOOLEAN DEFAULT FALSE,
created_at TIMESTAMP DEFAULT NOW(),
created_by INT DEFAULT 0,
updated_at TIMESTAMP DEFAULT NOW(),
updated_by INT DEFAULT 0
